{% extends 'base.html.twig' %}

{% block title %}Editer Gaming{% endblock %}

{% block body %}
    <h2 class="text-center mt-4 mb-4">Editer la partie de jeu</h2>
    <h3 class="text-center mt-4 mb-4">{{ gaming.nom }}</h3>
    {{ include('gaming/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="row">
        <div class="col">
            <div class="text-center mt-0">
                {{ include('gaming/_delete_form.html.twig') }}
                <a  href="{{ path('account_user',{'slug':app.user.slug}) }}">Retourner à mon profil</a>
            </div>
        </div>
    </div>
{% endblock %}
