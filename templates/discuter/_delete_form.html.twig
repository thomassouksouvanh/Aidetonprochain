<form method="post" action="{{ path('discuter_delete', {'slug': discuter.slug}) }}" onsubmit="return confirm('Veuillez confirmer la supresion');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discuter.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
