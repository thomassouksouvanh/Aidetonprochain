{% extends 'base.html.twig' %}

{% block title %}Edit Discuter{% endblock %}

{% block body %}
    <h2 class="text-center mt-4 mb-4">Editer la proposition</h2>
    <h3 class="text-center mt-4 mb-4">{{ discuter.sujet }}</h3>
    {{ include('discuter/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="row">
        <div class="col">
            <h6 class="text-center mt-0">
                {{ include('discuter/_delete_form.html.twig') }}
                <a href="{{ path('account_user',{'slug':app.user.slug}) }}">Retourner à mon profil</a>
            </h6>
        </div>
    </div>
{% endblock %}
