{% extends 'base.html.twig' %}

{% block title %}Edit proposition babysitting{% endblock %}

{% block body %}
    <h2 class="text-center mt-4 mb-4">Editer la proposition</h2>
    <h3 class="text-center mt-4 mb-4">{{ proposition.nom }}</h3>
    {{ include('proposition/babysitting/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="row">
        <div class="col">
            <h6 class="text-center mt-0">
                {{ include('proposition/babysitting/_delete_form.html.twig') }}
                <a href="{{ path('account_user') }}">Retourner à mon profil</a>
            </h6>
        </div>
    </div>
{% endblock %}

