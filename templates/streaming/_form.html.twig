{{ form_start(form ,{'attr': { 'id' : 'streaming' }}) }}
<div class="error-message text-center mb-2"></div>
    {{ form_row(form.nom, {'attr': {'class': 'formMain'}}) }}
    {{ form_row(form.description, {'attr': {'class': 'formMain'}}) }}
    {{ form_row(form.link, {'attr': {'class': 'formMain','id':'link'}}) }}

<div class="row">
    <button class="continue">{{ button_label|default('Continuer') }}</button>
</div>
{{ form_end(form) }}
